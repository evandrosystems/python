### My Python Project Template

Base template for Python projects using Docker.
This repository is used as a starting point for new Django, FastAPI, or others.

```
python -m venv .venv
```

```
source .venv/bin/activate
```

```
cp .env.example .env
```

```
docker compose run --rm python bash
```